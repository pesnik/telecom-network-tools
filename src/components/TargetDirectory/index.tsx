import Box from "@mui/material/Box";
import LinearProgress from "@mui/material/LinearProgress";

export default function TargetDirecoryViewer() {
  return (
    <Box sx={{ display: "none" }}>
      <LinearProgress />
    </Box>
  );
}
